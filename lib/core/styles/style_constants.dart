import 'package:flutter/material.dart';

const kShadowCardLight = [
  BoxShadow(
    color: Color.fromRGBO(0, 0, 0, 0.05),
    blurRadius: 7,
    spreadRadius: 0,
    offset: Offset(0, 2),
  )
];

const kBorderRadiusItem = BorderRadius.all(Radius.circular(10));
